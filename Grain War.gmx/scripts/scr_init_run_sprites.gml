///scr_init_run_sprites(image_speed,right,down,up)

// Set up the variables for running and standing sprites
run_image_speed = argument0;
run_right_image = argument1;
run_down_image = argument2;
run_up_image = argument3;

